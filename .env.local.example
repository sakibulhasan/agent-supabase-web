# API Configuration
# The backend API endpoint for the chatbot
# For Netlify deployment: Use /api/ask (proxied via netlify.toml)
# For local development with direct backend: Use http://localhost:8080/ask
NEXT_PUBLIC_API_URL=/api/ask

# Instructions:
# 1. Copy this file to .env.local
# 2. Update NEXT_PUBLIC_API_URL based on your setup:
#    - Production (Netlify): /api/ask
#    - Local with backend: http://localhost:8080/ask
# 3. Restart the development server for changes to take effect
